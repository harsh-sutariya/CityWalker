project:
  name: 'UrbanNav_Citywalk'
  run_name: 'slurm_sunny'
  result_dir: 'results'

training:
  batch_size: 8
  max_epochs: 5
  gpus: 1
  amp: false

scheduler:
  name: 'cosine'
  step_size: 10
  gamma: 0.1

optimizer:
  name: 'adamw'
  lr: 5e-4

model:
  do_transform: true
  obs_encoder:
    type: 'efficientnet-b0'
    context_size: 5

  cord_embedding:
    type: 'polar'
    num_freqs: 6
    include_input: true

  encoder_feat_dim: 512

  decoder:
    type: 'attention'
    len_traj_pred: 5
    num_heads: 2
    num_layers: 2
    ff_dim_factor: 4

data:
  type: citywalk
  video_dir: '/vast/xl3136/citywalk/videos/sunny_segments/'
  pose_dir: '/vast/xl3136/citywalk/poses/sunny_segments'
  num_workers: 6
  pose_fps: 5
  video_fps: 30
  target_fps: 1
  num_train: 3500
  num_val: 500
  num_test: 500
  input_noise: 0.1 # sigma (after normalization)
  search_window: 50 # frames
  arrived_threshold: 5 # num frame

testing:
  num_visualize: 50

logging:
  enable_wandb: true  # Set to false to disable Wandb logging
  pbar_rate: 100